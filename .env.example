ANTHROPIC_API_KEY=your-api-key-here

# Optional (backend): caps requests per IP on /ask, e.g. "10/hour", "5/minute".
# RATE_LIMIT=10/hour

# Optional (frontend): if set, visitors must enter this code to use the chat.
# CAMPUSIQ_ACCESS_CODE=
